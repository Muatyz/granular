function [tspan, Amp, fspan_half, DFT_half] = csvsRead(folderpath)
%%% e.g. generator.csv  received.csv are in noise_method
%%% so input "../include/response/noise_method"
namelist = dir(folderpath);
    for i = 1:length(namelist)
        
    end
end